let pi=Math.PI;

//Arrow() Function
let sum=()=>{
    return 2+3;
}

 let maxNum=Math.max(2,6,8,1,20);

let obj={
    name:"shri",
    age:20,
    address:"Shripewadi"
}
module.exports={pi,sum,maxNum,obj};